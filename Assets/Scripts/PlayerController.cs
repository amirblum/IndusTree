using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerController : MonoBehaviour
{
    public int playerID;
    private Vector2 position;

    // public void MoveCursor (Direction) {

    //     if  (direction == up) {
    //         Vector2 cursorPosition = Vector2(cursorPosition.x, cursorPosition.y + 1 )
    //     }

    // }
    
    // protected void Update()
    // {
    //    var success = GameBoard.Instance.PlaceTile(playerID, 0, -1);
    // }
}
